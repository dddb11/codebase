#include <bits/stdc++.h>
using namespace std;
int n;
int a[15];
int l[15],r[15];
int t = 0;
void work(int side){
	if(side == 2){
		
	}
}
int main(){
	memset(l,0,sizeof(l));
	memset(r,0,sizeof(r));
	cin >> n;
	for(int i=1;i<=n;i++) cin >> a[i];
	sort(a,a+n+1);
	work(2);
	return 0;
}
